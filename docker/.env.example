# ══════════════════════════════════════
# VOID — Docker Environment Variables
# Copy to .env and fill in your values
# ══════════════════════════════════════

# ═══ KHOJ ═══
KHOJ_ADMIN_EMAIL=admin@yourdomain.com
KHOJ_ADMIN_PASSWORD=change-this-secure-password
KHOJ_SECRET_KEY=generate-a-random-secret-key-here
KHOJ_DB_PASSWORD=change-this-db-password

# ═══ N8N ═══
N8N_DB_PASSWORD=change-this-db-password
N8N_ENCRYPTION_KEY=generate-a-random-encryption-key-here
N8N_WEBHOOK_URL=https://n8n.yourdomain.com

# ═══ CLAUDE / ANTHROPIC ═══
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ═══ VAULT ═══
# Path to Obsidian vault on the VPS
VAULT_PATH=/opt/void-vault

# ═══ TIMEZONE ═══
TIMEZONE=Asia/Dhaka

# ══════════════════════════════════════
# HOW TO GENERATE SECURE VALUES:
#
# On Mac/Linux:
#   openssl rand -hex 32
#
# On Windows (PowerShell):
#   [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])
#
# Or use a password manager to generate
# ══════════════════════════════════════
